# 3. Install an external module and use it to perform an operation of your interest.  
import pyttsx3

engine = pyttsx3.init()
engine.say('I have installed an external module and used it to perform text-to-speech conversion.')
engine.runAndWait()