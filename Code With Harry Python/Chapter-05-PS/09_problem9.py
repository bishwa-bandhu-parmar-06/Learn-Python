# 9. Can you change the values inside a list which is contained in set S? 
# s = {8, 7, 12, "Harry", [1,2]} 

s = {8, 7, 12, "Harry", [1,2]}

"""
No, 
you cannot change the values inside a list contained in a set
 — because Python doesn't even allow lists inside sets in the first place.
"""
# we can not include list inside set
